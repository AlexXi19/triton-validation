version: '3.9'

services:
  mongodb:
    image: mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - ./data/db:/data/db

  rabbitmq:
    image: rabbitmq
    restart: always
    ports:
      - "5672:5672"
